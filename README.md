Steps to Run

Place StudentPerformanceFactors.csv in the same directory as the script or notebook.

Run the notebook or Python script from top to bottom.

All visualizations will display automatically.

Model performance metrics and selected features will be printed to the console.
